interface LViewsTokens {
  table: {
    columns: {
      name: string;
      token: string;
    };
  };
  navActions: LNavActions;
  messageBox: {
    prompt: {
      create: {
        title: string;
        placeholder: string;
      };
    };
  };
}
